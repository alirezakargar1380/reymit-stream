import {
    IToolsTargetMainSettingsDefaultInput
} from "./../../../../interfaces/tools/target/tools.target.main_settings.interface"

const defaultSettings: IToolsTargetMainSettingsDefaultInput = {
    active: false,
    show_target: false,
    target_amount: 0,
    amount_until_now: 0,
    target_title: "",
}

export default defaultSettings